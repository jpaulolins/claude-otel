# OS/editor noise
.DS_Store
.idea/
.vscode/

# Environment and secrets (local only)
.env
.env.*
!.env.example
*.pem
*.key
go-hook-mcp-api/mcp-users.json

# Claude/Cursor/Gemini local state
.claude/settings.local.json
opencode.json
# Anchored to repo root so it does NOT match config-samples/gemini/.gemini/
/.gemini/
.ai-jail
.planning/

# Codereview pre-analysis artifacts (generated)
go-hook-mcp-api/docs/codereview/

# Go build/test artifacts
go-hook-mcp-api/bin/
*.test
*.out
coverage.out
cover.out

# Runtime logs and temp files
*.log
tmp/
temp/
.worktrees/

# Metabase ClickHouse driver JAR (downloaded by metabase/fetch-driver.sh)
metabase/plugins/*.jar

# Metabase admin password (generated by start.sh up-metabase; delete to rotate)
metabase/.admin-password
